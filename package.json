{
  "name": "bnload",
  "devDependencies": {
    "turbo": "^2.5.8",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "examples/*"
  ]
}

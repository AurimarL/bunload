export default function BoilerplateDescription() {
  return (
    <p className="text-gray-400 mt-12">
      This boilerplate connects these powerful tools into a single starter kit,
      so you can ship full-stack apps with authentication, file handling, and
      content management — in minutes.
    </p>
  );
}

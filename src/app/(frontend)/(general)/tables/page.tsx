import DataTable from "@/components/(general)/(tables)/data-table/data-table";

export default function TablePage() {
  return (
    <main className="flex flex-1 flex-col gap-4 p-4 pt-0">
      <DataTable />
    </main>
  );
}
